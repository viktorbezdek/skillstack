Saved to `audit.md`. Six issues found:

1. **Dead reference** — step 4 points to `references/full-guide.md` which doesn't exist; remove or replace with an inline checklist.
2. **Blocked tools** — `WebFetch` and `WebSearch` are hook-intercepted; strip both from `allowed-tools`.
3. **Circular description** — "Use this skill to review code" restates the name; rewrite to give a real trigger signal.
4. **Non-actionable steps** — "Look at the code" isn't executable; replace with concrete directives (read files, grep for tests, check specific patterns).
5. **Generic tips** — "Be thorough" adds nothing; replace with named checks (e.g., "flag missing edge-case tests by name").
6. **Missing exclusion clause** — no "do NOT invoke for…" guard, so the skill may trigger on prose/doc-only changes.